import stitches from "../../stitches"

export const Stack = stitches.styled("div", {
  display: "flex",
  flexDirection: "column",
})

export const HStack = stitches.styled("div", {
  display: "flex",
  flexDirection: "row",
})
